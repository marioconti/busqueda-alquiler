# Datos scrapeados: 54 MB y se regeneran con `python scripts/main.py`.
data/avisos.json
data/ids_vistos.json
data/raw/
data/logs/
data/propiedades.csv

# Fotos locales: 36 MB. Solo sirven para que Claude mire y clasifique el estado;
# la pagina publicada muestra las remotas del CDN.
web/fotos/

# Capturas de trabajo.
web/_vista-*.png

# web/data.js SI se versiona: es lo unico que hace andar la pagina publicada, y desde
# que build.py deja pasar solo lo que app.js dibuja pesa 11 MB en vez de 52.

__pycache__/
*.pyc
